import { Router } from '../Router';

/**
 * Реализация маршрутизатора для нативных приложений.
 */
export class NativeRouterAdapter extends Router {
  constructor() {
    super();
    // Инициализация маршрутизатора для нативных приложений
  }

  on(path, handler) {
    // Реализация добавления маршрута для нативных приложений
  }

  start() {
    // Реализация запуска маршрутизатора для нативных приложений
  }
}
